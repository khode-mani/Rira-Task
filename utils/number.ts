// // utils/number.ts
// export const parseNumber = (raw?: string | number) => {
//   if (raw == null) return NaN
//   return typeof raw === "number" ? raw : Number(String(raw).replace(/[,\s]/g, ""))
// }

// export const tomanToRial = (n: number) => n * 10

// // export const parseNumber = (raw: string) =>
// //   Number(String(raw).replace(/[^\d.]/g, "")) // رقم و نقطه

// // export const tomanToRial = (n: number) => n * 10